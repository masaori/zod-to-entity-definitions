# NPM Registry Configuration
# This file configures npm for publishing the package

# Use the default npm registry
registry=https://registry.npmjs.org/

# Save exact versions in package.json (not using ^ or ~)
save-exact=true

# Package access (set to public for open source packages)
access=public

# Authentication is handled via GitHub Actions OIDC (Trusted Publishing)
# The actions/setup-node action automatically configures authentication
# when registry-url is specified and id-token: write permission is set
