# NPM Registry Configuration
# This file configures npm for publishing the package

# Use the default npm registry
registry=https://registry.npmjs.org/

# Save exact versions in package.json (not using ^ or ~)
save-exact=true

# Package access (set to public for open source packages)
# Uncomment the line below if publishing a scoped package
# access=public
